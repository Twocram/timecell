export type UserTask = {
  summary: string
  time: string[]
  color: string
  selectedTime: string
  option: string
}

export type ComboboxOption = {
  label: string;
  value: string | number;
}